#include <stdio.h>
void swap( int*a,int*b)
{
    int temp;
    temp=*a;
    *a=*b;
    *b=temp;
    printf("%d %d",*a,*b);
}
int main()
{
    int*k,*p,n,m;
    printf("enter two numbers");
    scanf("%d %d",&n,&m);
    p=&n;
    k=&m;
    swap(p,k);
    return 0;
}
